[Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
if (!([Security.Principal.WindowsPrincipal][Security.Principal.WindowsIdentity]::GetCurrent()).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)) {
    Write-Host "[!] SwiftHub Ekosistemi tam yetki gerektirir. UAC onayi bekleniyor..." -ForegroundColor Yellow
    Start-Process powershell -ArgumentList "-NoProfile -ExecutionPolicy Bypass -Command `"irm aydinaydmr.com.tr/core | iex`"" -Verb RunAs
    exit
}
# Pure RAM Execution (Zero-Footprint Ultimate)
irm "https://raw.githubusercontent.com/cyberQbit/SwiftHub/main/SwiftHub.ps1?t=$((Get-Date).Ticks)" | iex